[pytest]
# Modules live in hyphenated package dirs (youtube-research), which pytest's
# default prepend import mode can't infer; importlib mode imports each test file
# in isolation.
addopts = --import-mode=importlib
