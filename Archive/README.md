# Archive

Superseded modules, kept for history. Not discovered or installed (they live
outside `modules/`).

- **model-cost** — folded into the `tokenpulse` module on 2026-09-07. TokenPulse
  now covers per-model spend, budgets, credit balances, and key limits in one
  module with click-through provider detail. Install `tokenpulse` instead.
